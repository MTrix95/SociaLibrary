# MICROSERVIZI BE
services:
  gateway:
    build: ../GatewayService
    env_file:
      - ./environment/.env.local
    environment:
      GATEWAY_HOST_DB: ${GATEWAY_HOST_DB}
      GATEWAY_USER_DB: ${GATEWAY_USER_DB}
      GATEWAY_PASSWORD_DB: ${GATEWAY_PASSWORD_DB}
    ports:
      - "8083:8080"
    depends_on:
      keycloak:
        condition: service_healthy
      db:
        condition: service_healthy
    healthcheck:
      test: [ "CMD-SHELL", "curl --fail --silent http://localhost:8080/actuator/health | grep UP || exit 1" ]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - net

  administrator:
    build: ../AdministratorService
    env_file:
      - ./environment/.env.local
    environment:
      ADMINISTRATOR_HOST_DB: ${ADMINISTRATOR_HOST_DB}
      ADMINISTRATOR_USER_DB: ${ADMINISTRATOR_USER_DB}
      ADMINISTRATOR_PASSWORD_DB: ${ADMINISTRATOR_PASSWORD_DB}
    ports:
      - "8084:8080"
    depends_on:
      gateway:
        condition: service_healthy
    healthcheck:
      test: [ "CMD-SHELL", "curl --fail --silent http://localhost:8080/actuator/health | grep UP || exit 1" ]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - net

  library:
    build: ../LibraryService
    env_file:
      - ./environment/.env.local
    environment:
      LIBRARY_HOST_DB: ${LIBRARY_HOST_DB}
      LIBRARY_USER_DB: ${LIBRARY_USER_DB}
      LIBRARY_PASSWORD_DB: ${LIBRARY_PASSWORD_DB}
      SHARED_DATA_THUMBNAILS: ${SHARED_DATA_THUMBNAILS}
    ports:
      - "8085:8080"
    volumes:
      - ${SHARED_DATA_THUMBNAILS:-./locale/storage/thumbnails}:/app/thumbnails:rw
    depends_on:
      gateway:
        condition: service_healthy
    healthcheck:
      test: [ "CMD-SHELL", "curl --fail --silent http://localhost:8080/actuator/health | grep UP || exit 1" ]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - net